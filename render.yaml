services:
  - type: web
    name: social-sandbox-api-proxy
    runtime: node
    plan: free
    buildCommand: cd backend && npm install
    startCommand: cd backend && npm start
    envVars:
      - key: OPENAI_API_KEY
        scope: run
        sync: false
